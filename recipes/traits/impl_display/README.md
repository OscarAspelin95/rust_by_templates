# Problem
You want to derive the `Display` trait for enabling `println!("{}", s)` for a struct `s`.

# Solution
Implement the `Display` trait for this struct.

# Notes
In this example, we define a struct for which we implement the `Display`.
