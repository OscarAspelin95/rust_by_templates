# Problem
You have a custom data structure for which you'd like to use `.into` and `::from`.

# Solution
Implement the `Into` and `From` traits.

# Notes
In this example, we define two temperature structs `Celsius` and `Farenheit` for which we implement functionality to convert between the two.
