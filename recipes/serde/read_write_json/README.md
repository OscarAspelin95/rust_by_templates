# Problem
You need to read and write datastructures to json.

# Solution
Use serde and serde_json.

# Notes
In this example, we use `serde` and `serde_json` to write a struct to a json file and then read it back as a struct again.
