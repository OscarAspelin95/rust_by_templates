# Problem
You need to add tracing to an HTTP server.

# Solution
Use `tracing` and `tracing-subscriber`.

# Notes
Run `cargo run` and use your favorite API tool to send requests to `http://localhost:8080/health`.
